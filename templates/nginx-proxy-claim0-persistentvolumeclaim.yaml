apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    labels:
        io.kompose.service: nginx-proxy-claim0
    name: nginx-proxy-claim0
spec:
    accessModes:
        - ReadOnlyMany
    resources:
        requests:
            storage: 100Mi
