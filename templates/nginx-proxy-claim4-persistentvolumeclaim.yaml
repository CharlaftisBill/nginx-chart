apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    labels:
        io.kompose.service: nginx-proxy-claim4
    name: nginx-proxy-claim4
spec:
    accessModes:
        - ReadOnlyMany
    resources:
        requests:
            storage: 100Mi
