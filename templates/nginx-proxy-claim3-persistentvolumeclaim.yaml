apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    labels:
        io.kompose.service: nginx-proxy-claim3
    name: nginx-proxy-claim3
spec:
    accessModes:
        - ReadOnlyMany
    resources:
        requests:
            storage: 100Mi
